// getting input from the file

using namespace std;
#include <bits/stdc++.h>
#include <iostream>
#include <sstream>
#include <fstream>
#include <string>


int main() {
  std::ifstream input_file("day 4 aoc.txt");
  std::cin.rdbuf(input_file.rdbuf());
  std::string x;
  while (std::cin >> x) {
    std::cout << x << std::endl;
  }
  input_file.close();
  return 0;
}
